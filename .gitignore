# Xcode
build/
DerivedData/
*.xcuserstate
xcuserdata/
*.xcscmblueprint
*.xccheckout

# Swift Package Manager
.swiftpm/
.build/
.spm-cache/

# 测试覆盖率输出（vitest --coverage 产物；server / 网页端通用，绝不入库。子目录 .gitignore 已含 node_modules/dist）
coverage/

# XcodeGen 生成的工程（可由 project.yml + `xcodegen generate` 重建）
# 新手如果想用 git 跟踪 .xcodeproj，删掉下面这一行即可。
BeeUrEi.xcodeproj/

# macOS
.DS_Store

# 密钥/环境变量（绝不提交）
.env
*.env
!*.env.example
# APNs 私钥（.p8）——绝不提交
*.p8
AuthKey_*.p8
**/.env
*.xcconfig
Secrets.plist

# Local agent/editor state
.claude/

# 运行期产物：在仓库根误跑 server / vitest 时落下的 SQLite 库、媒体留存与 node/vite 缓存——绝不入库
# （server/ 子目录已各自忽略，这里兜住"在根目录跑"时产生的同名目录）
/data/
/node_modules/

# 本地 vendored 二进制框架（WebRTC.xcframework，91MB，绝不入库——见 docs A2）
Frameworks/
extrapic/
